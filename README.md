# Debian based distros post install
Script de pós instalação de distros baseadas no Debian (gerenciador de pacotes apt)


## Como usar

Execute o comando abaixo no terminal

```shell
sudo bash debian-post-install.sh
```